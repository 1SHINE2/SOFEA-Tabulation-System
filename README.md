# SOFEA Tabulation App

## Setup
1. Copy `.env.local.example` to `.env.local` and fill in your Firebase credentials
2. Run `npm install`
3. Run `npm run dev`

## Deployment
Push to GitHub → auto-deploy via Vercel
